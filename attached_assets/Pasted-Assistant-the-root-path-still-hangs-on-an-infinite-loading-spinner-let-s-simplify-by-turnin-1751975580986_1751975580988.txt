Assistant, the root path (`"/"`) still hangs on an infinite loading spinner—let’s simplify by turning it into a **static Landing page** and moving the dashboard behind `"/dashboard"`. Overwrite or create files as needed:

---

## 1. Create a Landing Page Component

**File:** `src/pages/Landing.tsx`

```tsx
import { Link } from 'react-router-dom';

export default function Landing() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-gray-50 dark:bg-gray-900">
      <img src="/assets/logo-full.svg" alt="DukaSmart" className="h-16 mb-6" />
      <h1 className="text-4xl font-bold text-gray-800 dark:text-gray-100 mb-4">
        Welcome to DukaSmart
      </h1>
      <p className="text-lg text-gray-600 dark:text-gray-300 mb-8 text-center max-w-lg">
        Effortlessly manage your duka’s inventory, sales, and customers—all in one mobile‑first POS.
      </p>
      <div className="space-x-4">
        <Link
          to="/login"
          className="px-6 py-3 bg-primary text-white rounded hover:bg-primary-dark"
        >
          Sign In
        </Link>
        <Link
          to="/signup"
          className="px-6 py-3 border border-primary text-primary rounded hover:bg-primary hover:text-white"
        >
          Sign Up
        </Link>
      </div>
    </div>
  );
}
2. Update Routing in App.tsx
Replace the root route to load Landing instead of the protected dashboard:

diff
Copy
Edit
--- src/App.tsx
+++ src/App.tsx
@@
- import Dashboard from './pages/Dashboard';
+ import Landing from './pages/Landing';
+ import Dashboard from './pages/Dashboard';
@@
   <Routes>
-    <Route path="/" element={user ? <Dashboard /> : <Landing />} />
+    <Route path="/" element={<Landing />} />
     <Route path="/login" element={<LoginPage />} />
     <Route path="/signup" element={<SignUpPage />} />
-    <Route element={<MainLayout />}>
-      <Route path="/inventory" element={<Inventory />} />
-      <Route path="/customers" element={<Customers />} />
-    </Route>
+    <Route element={<MainLayout />}>
+      <Route path="/dashboard" element={<Dashboard />} />
+      <Route path="/inventory" element={<Inventory />} />
+      <Route path="/customers" element={<Customers />} />
+    </Route>
   </Routes>
3. Adjust Navigation & Redirects
Ensure your sidebar/home links now point to "/dashboard" instead of "/".

In any redirects (e.g. on successful login), navigate to "/dashboard".

4. QA Checklist
Visiting "/" shows the new static Landing page.

No infinite loading spinner on the root.

“Sign In” and “Sign Up” buttons correctly route to those pages.

After login, you land on "/dashboard" and the protected layout works as before.

Please apply these changes so the first page is a simple, static Landing page—eliminating the infinite‑loading issue.








Ask ChatGPT
